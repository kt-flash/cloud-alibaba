spring:
  cloud:
    config:
      #需要从github上获取的资源名称，注意没有yml后缀
      name: flash-config-client
      #本次访问的配置项
      profile: dev
      label: master
      #通过config server获取
      uri: http://config-3344.com:3344